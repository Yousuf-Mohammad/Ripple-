# Base URL of the ripple-api backend. No /api prefix.
# Android emulator → http://10.0.2.2:4000
# Physical device  → http://<your-LAN-IP>:4000 (same Wi-Fi as the dev machine)
EXPO_PUBLIC_API_BASE_URL=http://10.0.2.2:4000
