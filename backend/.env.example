NODE_ENV=development
PORT=4000
MONGODB_URI=mongodb://localhost:27017/mini_social_feed
JWT_SECRET=replace_me
JWT_EXPIRES_IN=7d

# Comma-separated list of allowed CORS origins (optional; omit for permissive dev mode)
CORS_ORIGIN=

# Firebase Admin service account (optional until push notifications, phase P4)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
